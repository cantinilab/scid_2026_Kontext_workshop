# scid_2026_workshop/pyproject.toml
[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "kontext"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []          # provided by the conda env

[tool.setuptools.packages.find]
where = ["src"]